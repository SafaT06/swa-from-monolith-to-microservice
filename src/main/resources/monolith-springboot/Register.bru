meta {
  name: Register
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/auth/register
  body: json
  auth: inherit
}

body:json {
  {"username":"rody", "password": "rody"}
}

settings {
  encodeUrl: true
}
